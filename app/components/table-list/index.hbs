<h1>All Tables</h1>

<LinktTo @route="table-new" role="button">New Table</LinkTo>

<ul>
  {{#each @tables as |table|}}
  <li>
    <LinkTo @route="table-edit" @model={{table}}>{{table.name}}</LinkTo>
  </li>
  {{/each}}
</ul>
